apply hashcode, hashcode;
apply {
    User.Profile: hashcode, hashcode;
}

public class User {
    private String name;
    private int age;

    public class Profile {
        String nickname;
    }
}