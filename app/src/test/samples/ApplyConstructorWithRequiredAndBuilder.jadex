apply constructor(required), builder;
apply {
    User.Profile: constructor(required), builder;
}

public class User {
    private String name;
    private final int age;

    public static class Profile {
        String nickname;
        final int age;
    }
}